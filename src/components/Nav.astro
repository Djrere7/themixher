---
import Wordmark from './Wordmark.astro';
import { nav, site } from '../data/site';
const path = Astro.url.pathname;
const isActive = (href: string) => href !== '/' && path.startsWith(href);
---
<header class="nav" data-nav>
  <div class="wrap nav__row">
    <Wordmark height={46} />

    <nav class="nav__links" aria-label="Primary">
      {nav.map((item) => (
        <a href={item.href} class:list={['nav__link', { on: isActive(item.href) }]}>{item.label}</a>
      ))}
    </nav>

    <div class="nav__cta">
      <a href={site.donateUrl} class="btn btn-primary nav__donate">Donate</a>
    </div>

    <button class="nav__burger" aria-label="Open menu" aria-expanded="false" data-burger>
      <span></span><span></span><span></span>
    </button>
  </div>

  <div class="nav__mobile" data-mobile hidden>
    {nav.map((item) => (
      <a href={item.href} class:list={['nav__mlink', { on: isActive(item.href) }]}>{item.label}</a>
    ))}
    <div class="nav__mrow">
      <a href={site.donateUrl} class="btn btn-primary">Donate</a>
      <a href={site.sponsorUrl} class="btn btn-ghost">Sponsor</a>
    </div>
  </div>
</header>

<style>
  .nav { position: sticky; top: 0; z-index: 50; transition: background 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease; border-bottom: 1px solid transparent; }
  .nav.stuck { background: rgba(255, 253, 251, 0.86); backdrop-filter: blur(14px); border-bottom-color: var(--card-brd); box-shadow: 0 6px 24px -18px rgba(117,56,234,0.3); }
  .nav__row { display: flex; align-items: center; gap: 1.5rem; min-height: 76px; }
  .nav__links { display: flex; gap: 1.5rem; margin-left: auto; }
  .nav__link { font-size: 0.92rem; font-weight: 600; color: var(--muted); position: relative; padding: 0.3rem 0; transition: color 0.2s ease; }
  .nav__link::after { content: ''; position: absolute; left: 0; bottom: -2px; height: 2px; width: 0; background: linear-gradient(90deg, var(--orange), var(--orchid)); transition: width 0.25s ease; }
  .nav__link:hover, .nav__link.on { color: var(--ink); }
  .nav__link:hover::after, .nav__link.on::after { width: 100%; }
  .nav__cta { margin-left: 0.4rem; }
  .nav__donate { padding: 0.55rem 1.25rem; font-size: 0.9rem; }

  .nav__burger { display: none; background: none; border: 0; cursor: pointer; padding: 0.4rem; margin-left: auto; }
  .nav__burger span { display: block; width: 26px; height: 2px; background: var(--ink); margin: 5px 0; border-radius: 2px; transition: transform 0.25s ease, opacity 0.2s ease; }
  .nav__burger[aria-expanded='true'] span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .nav__burger[aria-expanded='true'] span:nth-child(2) { opacity: 0; }
  .nav__burger[aria-expanded='true'] span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  .nav__mobile { display: none; flex-direction: column; gap: 0.2rem; padding: 0.5rem var(--gutter) 1.5rem; background: rgba(255,253,251,0.98); backdrop-filter: blur(14px); border-bottom: 1px solid var(--card-brd); }
  .nav__mlink { padding: 0.8rem 0; font-family: var(--sans); font-weight: 800; font-size: 1.3rem; text-transform: uppercase; color: var(--muted); border-bottom: 1px solid var(--card-brd); }
  .nav__mlink.on { color: var(--ink); }
  .nav__mrow { display: flex; gap: 0.8rem; margin-top: 1.1rem; }
  .nav__mrow .btn { flex: 1; }

  @media (max-width: 960px) {
    .nav__links, .nav__cta { display: none; }
    .nav__burger { display: block; }
    .nav__mobile[data-open] { display: flex; }
  }
</style>

<script>
  const nav = document.querySelector('[data-nav]');
  const onScroll = () => nav?.classList.toggle('stuck', window.scrollY > 12);
  onScroll();
  window.addEventListener('scroll', onScroll, { passive: true });

  const burger = document.querySelector('[data-burger]');
  const mobile = document.querySelector('[data-mobile]') as HTMLElement | null;
  burger?.addEventListener('click', () => {
    const open = burger.getAttribute('aria-expanded') === 'true';
    burger.setAttribute('aria-expanded', String(!open));
    if (mobile) {
      if (open) { mobile.removeAttribute('data-open'); mobile.hidden = true; }
      else { mobile.hidden = false; mobile.setAttribute('data-open', ''); }
    }
  });
</script>
